# Context Notes

`/context` is treated as the primary reference location for this repository.

In this checkout, upstream Uniswap v4 references are consumed through pinned submodules under `lib/v4-periphery` and `lib/v4-periphery/lib/v4-core`.

If external context files are provided later, place them under:

- `context/uniswap/`
- `context/atrium/`
