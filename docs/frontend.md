# Frontend

The React + TypeScript dashboard in `frontend/` consumes ABIs/constants from `shared/`.

Supported actions:
- wallet connect
- chain profile update
- per-pool policy update
- router allowlist / actor denylist updates
- CLI command helpers for deploy + demo scripts

Run:
```bash
npm install
npm run dev --workspace frontend
```
